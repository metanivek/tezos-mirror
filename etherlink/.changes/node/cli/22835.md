- Add support for the Etherlink 7 kernels, `ganesha` and `ganesha-r1`: they can
  be downloaded by name with `download kernel <name>`, and named through
  `--kernel-compat <name>`. Native execution is not included.
